apiVersion: v1
kind: Service
metadata:
  name: edge-runtime
spec:
  selector:
    app: edge-runtime
  ports:
    - protocol: TCP
      port: 8087
      targetPort: 8087
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: edge-runtime
  labels:
    app: edge-runtime
    tracing: enabled
    federation: enabled
  annotations:
    sovereign-runtime: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: edge-runtime
  template:
    metadata:
      labels:
        app: edge-runtime
        tracing: enabled
        federation: enabled
      annotations:
        sovereign-runtime: "true"
    spec:
      containers:
        - name: edge-runtime
          image: liceu/edge-runtime:latest
          ports:
            - containerPort: 8087
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
            requests:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8087
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8087
            initialDelaySeconds: 10
            periodSeconds: 20
      automountServiceAccountToken: true
