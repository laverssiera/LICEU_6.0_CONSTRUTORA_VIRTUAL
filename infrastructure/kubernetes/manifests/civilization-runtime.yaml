apiVersion: apps/v1
kind: Deployment
metadata:
  name: civilization-runtime
  labels:
    app: civilization-runtime
    tracing: enabled
    federation: enabled
  annotations:
    sovereign-runtime: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: civilization-runtime
  template:
    metadata:
      labels:
        app: civilization-runtime
        tracing: enabled
        federation: enabled
      annotations:
        sovereign-runtime: "true"
    spec:
      containers:
        - name: civilization-runtime
          image: liceu/civilization-runtime:latest
          ports:
            - containerPort: 8092
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
            requests:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8092
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8092
            initialDelaySeconds: 10
            periodSeconds: 20
      automountServiceAccountToken: true
---
apiVersion: v1
kind: Service
metadata:
  name: civilization-runtime
spec:
  selector:
    app: civilization-runtime
  ports:
    - protocol: TCP
      port: 8092
      targetPort: 8092
