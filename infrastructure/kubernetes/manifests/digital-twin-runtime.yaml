apiVersion: v1
kind: Service
metadata:
  name: digital-twin-runtime
spec:
  selector:
    app: digital-twin-runtime
  ports:
    - protocol: TCP
      port: 8085
      targetPort: 8085
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: digital-twin-runtime
  labels:
    app: digital-twin-runtime
    tracing: enabled
    federation: enabled
  annotations:
    sovereign-runtime: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: digital-twin-runtime
  template:
    metadata:
      labels:
        app: digital-twin-runtime
        tracing: enabled
        federation: enabled
      annotations:
        sovereign-runtime: "true"
    spec:
      containers:
        - name: digital-twin-runtime
          image: liceu/digital-twin-runtime:latest
          ports:
            - containerPort: 8085
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
            requests:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8085
            initialDelaySeconds: 10
            periodSeconds: 20
      automountServiceAccountToken: true
