apiVersion: apps/v1
kind: Deployment
metadata:
  name: holographic-runtime
  labels:
    app: holographic-runtime
    tracing: enabled
    federation: enabled
  annotations:
    sovereign-runtime: "true"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: holographic-runtime
  template:
    metadata:
      labels:
        app: holographic-runtime
        tracing: enabled
        federation: enabled
      annotations:
        sovereign-runtime: "true"
    spec:
      containers:
        - name: holographic-runtime
          image: liceu/holographic-runtime:latest
          ports:
            - containerPort: 8093
          resources:
            limits:
              cpu: "1"
              memory: "1Gi"
            requests:
              cpu: "500m"
              memory: "512Mi"
          readinessProbe:
            httpGet:
              path: /health
              port: 8093
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 8093
            initialDelaySeconds: 10
            periodSeconds: 20
      automountServiceAccountToken: true
---
apiVersion: v1
kind: Service
metadata:
  name: holographic-runtime
spec:
  selector:
    app: holographic-runtime
  ports:
    - protocol: TCP
      port: 8093
      targetPort: 8093
