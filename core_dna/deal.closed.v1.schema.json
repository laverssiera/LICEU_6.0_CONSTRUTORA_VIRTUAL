{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "deal.closed",
  "type": "object",
  "properties": {
    "deal_id": {"type": "string"},
    "value": {"type": "number"},
    "closed_at": {"type": "string", "format": "date-time"},
    "juridico_id": {"type": "string"}
  },
  "required": ["deal_id", "value", "closed_at", "juridico_id"]
}
