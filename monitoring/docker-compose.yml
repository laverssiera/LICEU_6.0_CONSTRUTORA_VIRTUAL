version: '3.7'

services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    volumes:
      - ./prometheus/prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    ports:
      - "3000:3000"
    environment:
      - GF_SECURITY_ADMIN_PASSWORD=admin
    depends_on:
      - prometheus
    volumes:
      - ./grafana/provisioning/:/etc/grafana/provisioning/

# Exemplo: adicione seus monolitos abaixo para facilitar testes locais
#  hubbackoffice:
#    build: ./liceu-6.0/monolitos/hubbackoffice
#    ports:
#      - "8001:8000"
#    environment:
#      - ...
